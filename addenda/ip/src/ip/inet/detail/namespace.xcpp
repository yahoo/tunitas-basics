// This is C++ 23 -*- c++ -*- -std=c++23 in the stylings of the S.C.O.L.D. design method.
// For terms and provenance see the LICENSE file at the top of the repository.
#divert <fpp>
namespace ip::inet::detail {
  //
  // These are
  //   imported into ip::inet::nameinfo for getnameinfo(...)
  //   imported into ip::inet::addrinfo for getaddrinfo(...)
  //
  // They declared here so both namespaces see "the same type"
  //
  enum class HOST { };
  enum class SERVICE { };
}
#endiv
