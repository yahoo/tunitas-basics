#include <iostream>
auto main() -> int {
  // This test shows that some code compiles and links.  That is all.
  std::cout << "OK (proves that each and every half_float SCOLD-module [header] compiles & links)\n";
  return 0;
}
