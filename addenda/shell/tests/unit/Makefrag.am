# how do we know we're running them all?
# at least see tests/unit/list
#
# unit::rigging::test::Runner will run all tests from the top level
#
# How do we know we got 'em all?
# grep -nH -e 'Stream.*"' */Fixture.xcpp
#
tests_unit_TESTS = \
  tests/unit/list.test \
  \
  tests/unit/external.test \
  tests/unit/internal.test \
  tests/unit/run.test \
  tests/unit/want.test \
  \
  tests/unit/all.test \
  $(end)
